"use client";
import { useState } from "react";
export default function StlInventory(){const[status,setStatus]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Replacing active FullMix inventory…');const r=await fetch('/api/admin/stl-inventory/import',{method:'POST',body:new FormData(e.currentTarget)});setStatus(JSON.stringify(await r.json(),null,2))}return <main className="min-h-screen bg-black p-8 text-stone-100"><h1 className="text-2xl font-black">GPMx STL-FM INVENTORY</h1><p className="mt-2 text-stone-400">Upload the current STL-FM CSV. Its supplied Track IDs, URLs, formats, match flags, playback fields, and metadata are preserved exactly. The import atomically replaces the active FullMix registry; prior imports remain archived.</p><form onSubmit={submit} className="mt-6 space-y-4"><input name="file" type="file" accept=".csv,text/csv" required/><button className="block rounded bg-amber-300 px-4 py-2 font-bold text-black">Replace active FullMix inventory</button></form><pre className="mt-6 whitespace-pre-wrap text-sm">{status}</pre></main>}

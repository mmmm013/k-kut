'use client'; import {useEffect,useState} from 'react'; type Item={disco_track_id:string;track_name:string;album:string;artist:string;resolved:boolean|null};
export default function StlListen(){const [data,setData]=useState<{items:Item[];total:number;resolved:number;unresolved:number}|null>(null);const [active,setActive]=useState<Item|null>(null);useEffect(()=>{fetch('/api/admin/stl-listen/queue').then(async r=>{const body=await r.json();if(!r.ok)throw new Error(body.detail||body.error||'Inventory load failed');return body}).then(setData).catch(error=>setData({items:[],total:0,resolved:null,unresolved:null,error:error.message} as any))},[]);return <main className="min-h-screen bg-black p-6 text-stone-100"><h1 className="text-2xl font-black">GPMx VOCAL LISTENING INVENTORY</h1><p className="mt-2 text-stone-400">Imported vocal masters only. Listening does not create, approve, or release a KUT.</p>{data&&<p className="mt-3">{data.total} vocal candidates. Select a track to resolve and play its private master.</p>}<div className="mt-5 grid gap-4 lg:grid-cols-[360px_1fr]"><aside className="max-h-[70vh] overflow-auto">{data?.items.map(item=><button key={item.disco_track_id}  onClick={()=>setActive(item)} className="mb-2 block w-full rounded border border-stone-700 p-3 text-left disabled:opacity-40"><b>{item.track_name}</b><br/><small>{item.artist} · {item.album} · private playback on selection</small></button>)}</aside><section className="rounded border border-stone-700 p-5">{active?<><h2 className="text-xl font-bold">{active.track_name}</h2><p className="my-2 text-stone-400">{active.artist} · {active.album}</p><audio controls autoPlay className="w-full" src={`/api/admin/stl-listen/audio/${encodeURIComponent(active.disco_track_id)}`}/></>:<p>Select a playable vocal master.</p>}</section></div></main>}
